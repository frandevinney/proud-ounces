{% set hed = "A Successful Proud Ounces Policy Will:" %}

<div class="policymaking-highlights pad-top-600 pad-bottom-600">
  <div class="inner-wrapper">
    <h3 class="ta-left inner-wrapper color-dark">{{ hed }} </h3>
    <div class="highlights-wrapper">
      {% for highlight in policyMakingHighlights %}
        <div>
          <span class="highlight-number">{{ loop.index }}</span>
          <span class="font-cooper-light text-500">{{ highlight }}</span>
        </div>
      {% endfor %}
    </div>
  </div>
</div>
